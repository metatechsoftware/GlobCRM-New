namespace GlobCRM.Domain.Enums;

/// <summary>
/// Enumerates the types of activities that can be created in the CRM.
/// Used for categorizing tasks, calls, and meetings.
/// </summary>
public enum ActivityType
{
    Task,
    Call,
    Meeting
}
