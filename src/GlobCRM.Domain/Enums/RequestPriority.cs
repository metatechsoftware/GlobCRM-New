namespace GlobCRM.Domain.Enums;

/// <summary>
/// Enumerates the priority levels of a request.
/// Used for sorting and filtering requests by urgency.
/// </summary>
public enum RequestPriority
{
    Low,
    Medium,
    High,
    Urgent
}
