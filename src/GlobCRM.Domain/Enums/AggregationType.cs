namespace GlobCRM.Domain.Enums;

/// <summary>
/// Aggregation functions for grouped report fields.
/// </summary>
public enum AggregationType
{
    Count = 0,
    Sum = 1,
    Average = 2,
    Min = 3,
    Max = 4
}
